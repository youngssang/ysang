상수   const constant ? (최대값 최소값)  01, 02, 03.c

한번 설정해두면 그 프로그램이 종료될 때 까지 결코 변경될 수 없는 값
상수를 숫자로 직접 나타내는 것보다 이름을 붙여 사용하는 것이 좋다.
상수 이름은 대문자로 관례적으로 사용.

연산자(operator) 연산에 사용되는 기호
항(operand) 연산에 사용되는 값

대입 연산자
변수에 값을 대입하는 연산자
연산의 결과를 변수에 대입
우선 순위가 가장 낮은 연산자

변수 값 교환하기

산술 연술자

% = 앞 항에서 뒤 항을 나누어 나머지를 구한다. (홀수, 짝수, 자리배치)

관계(비교) 연산자  math_op.c

== 두 항 값이 같으면 참. 아니면 거짓
!= 두 항 값이 다르면 참. 아니면 거짓 (다른거)
c언어에서는 연산의 결과 값을 0과 1로 나타냄

논리 연산자(나중에 조건에서 쓰임)  logic.op.c

&&  =  (7<3) && (5>2)  =   0 && 1   결과값이 1 && 1 일때만 결과가 1. 둘다 만족해야함
||  = (7>3) || (5<2)   0 && 0  결과값이 0 0 일때만 결과가 1. 둘다 0이어야
! (논리 부정)

비트 연산자  - 이진수로 바꾼다?

choice_op.c
조건 연산자 > 제어문 중 조건문을 간단히 표현할 때 사용(if문)
(5>3)?10:20;

scan.c / scan_ex.c
사용자의 값을 입력받는 함수(데이터 입력 받기)
scanf(입력 형식, 데이터 저장 변수);
int n = 10;
    scanf("%d", &n);
scanf 쓸때는 앞에 #define 넣기

0x%x     0x가 뭐지?  %p로 바꾸라는데 p는 머야
주소값이란게 뭘까

-----------
조건문(IF문)  if에는 항상 {} 사용
if, switch
if문
ex) if(5>3) {
     printf(~);
if-else문
ex) if(5>3) {
     pruntf(~);

홀수/짝수 판정하는 프로그램 만들기  if_es.c
학점 ABCDF 조건문 만들기  if_es2.c
학점을 판정하는 프로그램    scorepoint.c


cmd 작업

cd c:/   C로가기
cd devC  devC로가기
git commit -m "저장명"   파일 올리기(깃홈피)
git push      (넣기)

저장하기&불러오기
cd..
mkdir gitTest (폴더만들기)
cd gitTest (gitTest 폴더로 이동하기)

최초 불러오기 = git clone 주소(깃홈피)
기존 불러오기 = git pull

