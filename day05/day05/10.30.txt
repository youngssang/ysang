문자열 배열이 char 케릭터인가 ?


주소 연산자 [0]   [0]


포인터
int* p    포인터 (주소를 저장하는 변수)    p = &n
모든 메모리는 주소를 갖는데, 메모리 주소를 저장하기 위해 사용되는 변수를 포인터 변수라 한다.
char ch << ch가 뭐였지..?  
cp = 캐릭터 포인터
값을 알고(넣고) 싶을 때는 *을 넣기
포인터 자료형의 크기는 모두 4byte이다.

포인터에서 %x는 주소, %d는 값을 알 수 있다. 
그럼 &랑 * 구분하는 방법은 ?
&는 가르킬 때 쓴다 ?

배열과 포인터
