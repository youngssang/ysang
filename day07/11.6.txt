실행 exe파일 만들기
리턴 위에 system("Pause") 를 명시해줘야함

디버깅(검토 작업)
F9, F10, F11

버퍼와 버퍼링
버퍼=처리할 데이터를 임시로 저장하는 저장소

fopen
fclose
puts
fgetc
fputc

오류가 났을 때는 확인하려면 return -1;
종료하려면 return 0;

쓰는 건 상관없는데 읽기모드에서는 NULL을 넣어줘야함 if = (fp  == NULL) {
fp , &fp  << 주소 꼭 넣어줘야함 &    주소 넣을때는 항상 & 쓰기

매크로함수
#define SUB(x,y)(x-y)

조건부 컴파일