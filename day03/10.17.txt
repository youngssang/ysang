배열 4일차   char  ch
문자열 %s
%c
%d
%p
%x = 주소
null 비어있음 - \0      'A'  "ABC"  홑, 쌍 구분하기


배열에서는
scanf_s("" 뒤에 & 안들어감)    - 주소 ?
scanf_s("", xx, sizeof(xx));  로 변경 = (서식문자, 배열, 배열의 크기)
printf("", xx) 마찬가지
배열에서는 \n 안씀. 알아서 나눠짐
배열의 이름(a)은 배열의 시작 주소와 같다.
배열은 1이 아닌 0부터 시작한다.  0 = 1

주소 ! 중요. 포인트 할 때 필요함

a 를 b 로 저장한다 라고 하면
b = a   로 가야함.  저장될 곳이 왼쪽, 기존이 오른쪽


함수
서브루틴(subroutine) 또는 프로시저 라 불림 / 메소드
하나의 기능을 수행하는 일련의 코드 / 펑션 function
함수는 내용을 밑에 적게 되면 변수를 앞에도 적어줘야함.
return 으로 값을 정해줌.
void = return 불가 (가장 밑에, return 밑에 쓰임)


1.내장 함수 - 표준 라이브러리 함수
math.h - 절대값, 반올림, 버림 등  사용 할 수 있는게 많음.
2.사용자 정의 함수
-변수 사용에 주의
(1)지역 변수 - local variable - 제어문, 함수 {}에 사용(블럭을 벗어나면 소멸)
(2)전역변수 - global variable - 전체 범위에서 사용(프로그램 종료되면 소멸)
(3)정적변수 - static variable - 값을 공유, 누적(프로그램 종료시 소멸)
전역변수, 정적변수는 메모리 영역이 같은 곳에 위치. - 데이터 영역
지역변수, 매개변수 - 스택(stack) 영역

다큐멘테이션  C documentation - DevDocs 
https://devdocs.io/c

math.h   메스점헤더파일을 따와서 사용한다.
abs(절대값), round(반올림), floor(내림)
date and time < 도 많이 씀

자료형
time_t > long 으로 사용
time은 1970.1.1 자정 ~ 지금까지 시간을 초단위로 출력
rand > 게임에서 많이 씀 (랜덤)

내장 함수
time_ex.c  이해 필요

rand() 함수
rand 는 srand와 같이 씀. (C언어에서만. C++은 x)

포함 시 <stdio.h> / 따로 할 시 "stdio.h"